image: microsoft/dotnet-framework

stages:
    - build
    - test

variables:
    test: "Example.Test"

before_script:
    - "cd TrayTool"
    - "dotnet restore"

build:
    stage: build
    script:
        - "dotnet build"